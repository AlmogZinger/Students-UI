query StudentsQuery{
    students {
        name
        test {
            subject
            grade
        }
    }
}

mutation AddingStudent($g_name : String! , $g_birthDate : Date! ){
    addStudent(name: $g_name  ,birth:$g_birthDate){
        name
        birthDate
    }
}
